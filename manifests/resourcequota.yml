apiVersion: v1
kind: ResourceQuota
metadata:
  name: namespace-quota
  namespace: microservices
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "4Gi"
    limits.cpu: "8"
    limits.memory: "8Gi"
    pods: "20"
